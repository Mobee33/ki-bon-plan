<div class="containersearch">
  <div class="row">
      <%= form_tag activities_path, method: :get do %>
        <%= text_field_tag :query,
          params[:query],
          class: "form-control p-2",
          placeholder: "Find an activity"
        %>
        <%= submit_tag "Search", class: "btn btn-primary p-2" %>
      <% end %>
  </div>
</div>

<div class="grid-container1">
  <% @activities.each do |activity| %>
    <div class="grid-item">
      <%= link_to activity_path(activity), class:"img1-hover-zoom" do%>
      <div class="card-activity">
        <%= cl_image_tag activity.photos[0].key%>
        <div class="card-activity-infos">
          <div class="card-activity-description">
            <h2><%= activity.title %></h2>
            <p><%= activity.short_description %></p>
          </div>
          <h2 class="card-activity-pricing"><%= activity.address%><br> Rs:<%= activity.price %></h2>
        </div>
      </div>
      <% end %>
    </div>
  <% end %>
</div>
<br>

<div  class="mx-auto" style="width: 200px;">
  <%= link_to "Create an activity", new_activity_path %>
</div>
