<%= simple_form_for @activity do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.input :address %>
  Categories:
  <%= f.select :category, Activity::CATEGORY %>
  <%= f.input :price %>
  <%= f.input :photo, as: :file%>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
